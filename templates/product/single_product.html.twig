<div class="product-item text-center">
    <a href="{{path('app_product', {'slug': product.slug}) }}"> <img src="/images/{{ product.image }}" alt={{product.name}} class="img-fluid"/> </a>
    <h5> {{ product.name }} </h5>
    <span class="product-subtitle"> {{ product.subtitle }} </span>
    <span class="product-price"> {{ (product.price / 100)|number_format(2, '.', ',')}} € </span> </br>
    <a href="{{ path('add_to_cart', {'id': product.id}) }}" class="btn btn-primary mb-5"> Add to cart </a>
</div>